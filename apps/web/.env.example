# URL for the Trawl API.
TRAWL_API_URL=http://localhost:3100

# Bearer token used by the web app when calling the API.
# You can also reuse API_KEY if you prefer a single shared secret in your shell env.
TRAWL_API_KEY=change-me
